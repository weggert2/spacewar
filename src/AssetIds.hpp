#pragma once

enum class TextureId
{
    Background,
    PlayerShip,
    EnemyShip,
    PlayerBullet,
    EnemyBullet
};

enum class SoundId
{
};

enum class TextId
{
    Logo,
    Pause,
    GameOver,
};

enum class FontId
{
    Default,
    Menu,
};
