#pragma once

enum class TextureId
{
    Background,
    PlayerShip,
    EnemyShip,
    PlayerBullet,
    EnemyBullet
};

enum class SoundId
{
};

enum class MusicId
{
    StartScreen, // alter-ego
    Playing,  //space-game
    GameOver, //own the night
    Winning, // time-heals
};

enum class TextId
{
    Logo,
    Pause,
    Controls,
    Credits,
    Win,
    GameOver,
};

enum class FontId
{
    Logo,
    Menu,
    Pause,
};
