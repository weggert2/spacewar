#pragma once

enum class TextureId
{
    Background,
    PlayerShip,
    EnemyShip,
    PlayerBullet,
    EnemyBullet
};

enum class SoundId
{
};

enum class TextId
{
    Logo,
    Pause,
    Controls,
    Credits,
    GameOver,
};

enum class FontId
{
    Logo,
    Menu,
    Pause,
};
