#pragma once

enum class TextureId
{
    Background,
    PlayerShip,
    EnemyShip,
    PlayerBullet,
    EnemyBullet
};

enum class SoundId
{
};

enum class MusicId
{
};

enum class TextId
{
    Logo,
    Pause,
    Controls,
    Credits,
    Win,
    GameOver,
};

enum class FontId
{
    Logo,
    Menu,
    Pause,
};
