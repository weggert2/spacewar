#pragma once

enum class TextureId
{
    Background,
    PlayerShip,
    Bullet,
};

enum class SoundId
{
};

enum class TextId
{
    SplashScreen,
    Pause,
    GameOver,
};

enum class FontId
{
    Default,
    Menu,
};
